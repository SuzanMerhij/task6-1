import './Nav_icon.css';

const Nav_icon = (props) => {
  return (
    
     <>
        <i className={props.class}></i> 
        <i className={props.phone}></i>
        <i className={props.tele}></i>
    </>   
  )
}

export default Nav_icon
